# v2.1.16 Release Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the approved session/media work into one canonical v2.1.16 artifact and clear the known v2.1.15 Clippy blocker without regressing performance or viewport behavior.

**Architecture:** Apply the session and media plans first, then make the minimal telemetry lint fix, bump all current release identity references to 2.1.16, and run the full host/static/package gates before handoff.

**Tech Stack:** Rust 2024, Cargo/Clippy, existing Bash release scripts.

**Spec:** `docs/superpowers/specs/2026-09-10-v2.1.16-session-media-design.md`

## Global Constraints
- One canonical version: `2.1.16`.
- No RC/r1/r2 suffixes.
- Preserve v2.1.15 performance and right-rail viewport features.
- Host Rust verification is authoritative.

---

### Task 1: Clear known telemetry Clippy blocker

**Files:**
- Modify: `crates/aether-telemetry/src/lib.rs`

**Interfaces:**
- Behavior must remain identical; only `Default` implementation form changes.

- [ ] **Step 1: Run** `cargo clippy -p aether-telemetry --all-targets -- -D warnings` and confirm RED on `clippy::derivable_impls`.
- [ ] **Step 2: Add `#[derive(Default)]` to `LinuxTelemetrySampler` and remove the manual `impl Default`.**
- [ ] **Step 3: Run the same Clippy command and verify PASS.**
- [ ] **Step 4: Commit** `fix(telemetry): derive default for sampler`.

### Task 2: Version identity bump

**Files:**
- Modify current-version references across workspace Cargo manifests, scripts, tests, desktop metadata, README, and package scripts.
- Preserve historical CHANGELOG entries.

**Interfaces:**
- Produces: `Aether Browser 2.1.16` binary identity and package `2.1.16-1`.

- [ ] **Step 1: Add a new v2.1.16 CHANGELOG entry** summarizing persistent logins, Private ephemerality, Twitch live/VOD reliability, YouTube/YouTube Music playback reliability, watchdog, telemetry Clippy repair, and carried performance/view-reclaim work.
- [ ] **Step 2: Bump current release references from 2.1.15 to 2.1.16** without rewriting historical CHANGELOG lines.
- [ ] **Step 3: Run release-identity and clean-break contracts.**
- [ ] **Step 4: Commit** `release: prepare aether browser 2.1.16`.

### Task 3: Full verification

**Files:** none beyond generated verification outputs.

- [ ] **Step 1: Run** `cargo fmt --all -- --check`.
- [ ] **Step 2: Run** `cargo check --workspace`.
- [ ] **Step 3: Run** `cargo clippy --workspace --all-targets -- -D warnings`.
- [ ] **Step 4: Run** `cargo test --workspace`.
- [ ] **Step 5: Run every `tests/current-*.sh` contract and require zero failures.**
- [ ] **Step 6: Run media runtime probes with bounded deadlines; require no endless-buffering state.**
- [ ] **Step 7: Run `scripts/package-release.sh /mnt/data` and inspect static/package verification for PASS.**
- [ ] **Step 8: Extract the freshly generated source ZIP and rerun all current static contracts from that exact archive.**
- [ ] **Step 9: Run consolidated installer `--verify`; require payload/checksum PASS.**
- [ ] **Step 10: Record host-only gates honestly; do not claim playback on real Twitch/YouTube succeeded unless host runtime evidence exists.**

### Task 4: Handoff

**Files:**
- Generate: `/mnt/data/Aether-Browser-v2.1.16-ASAP-TONIGHT.run`
- Generate: `/mnt/data/Aether-Browser-v2.1.16-INSTALL.run`
- Generate: `/mnt/data/Aether-Browser-v2.1.16-source.zip`
- Generate: `/mnt/data/Aether-Browser-v2.1.16-FINAL-VERIFY.txt`
- Generate: `/mnt/data/Aether-Browser-v2.1.16-SHA256SUMS.txt`

- [ ] **Step 1: Compute SHA-256 of all canonical artifacts.**
- [ ] **Step 2: Provide one `~/Downloads` command using the ASAP wrapper.**
- [ ] **Step 3: Ask for v2.1.16 VERIFY and INSTALL-VERIFY records after the host run.**
