[package]
name = "aetherforge-plasma-shell"
version = "1.0.0"
edition = "2021"
description = "KWin configuration generator for AetherForge (offline mode)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
shellexpand = "3.1"
dirs = "5.0"
